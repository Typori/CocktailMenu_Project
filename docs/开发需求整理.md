# 新需求
- 仪表盘里的“管理原料”和“查看酒单”按钮，居然还能进入以前老版本的管理页面，这个老页面布局可以移除了（注意检查是否会影响到其他功能调用），然后把这里的按钮链接到新的管理页面去。
- 设置撤销按钮
- 确保店面管理里的价格，是独属于这个店面的数据，并且在PDF导出时也要附上这个正确的信息。
- 进入仪表盘后，数据等了一下才刷新，说明算的很慢，需要优化计算方式，让用户体验好一点。
- PDF还是有分页在不该断开的地方断开的问题。

我需要在配方库的筛选功能中，新增一个原料筛选功能，用户可以筛选出使用了某几种原料的配方，在使用杯型的下方一行，提供一个区域，来从原料库添加原料，选择原料的功能完全继承配方编辑中原则原料的功能即可。

11.16

- PDF导出最后的原料汇总清单，希望根据原料类别进行展示，以进行区分。
- PDF导出的详细信息部分，希望能跟基本信息部分那种布局类似，以优化空间占用，将能在一行显示的内容放到一行里去，但要保证排版整齐，最好是跟编辑配方界面的横向、纵向布局保持一致。

配方编辑界面中，我发现刚才开发的时候，我漏掉了原本那个“菜单名称”的数据和功能，请把这块照着原样加回来，位置放在高级信息区域的最底部。

好了，现在，我要同步修订PDF导出的页面布局。
- 之前如果PDF的导出布局中，除了分页功能需要保留，其他功能先都移除。
- PDF导出完全按照配方浏览界面的信息，原封不动地呈现就行。
- 记得要有页码。
- 页眉不需要。
- 以上对PDF导出的修改，要同时应用于单个配方导出，和店面酒单导出。


配方浏览页面进行重新布局：
- 名称：保持原状
- 图片展示区：现在是展示一张图，如果尺寸不合适会以放大的方式展示，这导致很多竖向的图的信息展示不全，我希望这里的区域还是这么大，但展示图是一个竖向的展示区域，贴左侧展示，如果有多张图，根据浏览器的页面宽度自动调节展示几张。
- 剩下的内容分块与布局，跟编辑页面保持一致，但所有数据不可修改，且文本风格跟现在保持一致即可。

配方编辑页面的数据布局进行重新整理，重新分为如下模块：
- 基本信息：包含配方名称中英文，容量、酒精度，以及成品图片（把名字改成叫“附图”）
- 配料列表：保持原状
- 制作步骤：布局保持原状。但需要增加一个像原料列表一样，可以随时挪动不同步骤排序的功能，注意拖动后排序的编号也要随之改变。
- 高级信息：其内容和布局如下
风味标签
引用类型、颜色、使用杯型
建议售价、成本计算（用已有公式自动计算得到，不要新增计算量）、利润率（自动计算得到，如已有计算公式，不要新增计算量）
酒款描述。
- 备注

注意，如果对数据库的老版本数据有影响，需要在设置里的“运行数据修复”里，提供修复功能。


# 已完成需求
