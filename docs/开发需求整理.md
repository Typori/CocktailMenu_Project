# 新需求
- 仪表盘里的“管理原料”和“查看酒单”按钮，居然还能进入以前老版本的管理页面，这个老页面布局可以移除了（注意检查是否会影响到其他功能调用），然后把这里的按钮链接到新的管理页面去。
- 设置撤销按钮
- 进入仪表盘后，数据等了一下才刷新，说明算的很慢，需要优化计算方式，让用户体验好一点。
- PDF还是有分页在不该断开的地方断开的问题。
- 配方编辑页面，保存后会返回上一个界面，但当上一个界面是配方预览界面时，再点击返回，按现在的逻辑会返回到配方编辑界面，这不合理，应该返回到？？
- 已评分内容，是否应该支持修改呢？

# 11.20
记录
- 我刚复制了一款酒，发现复制出来的平均分还跟之前的评分一样，这样不对，应为各条评分的数据不应该被复制过来，所以复制出来的配方，评分应该是根据自己的评价内容里的分来算的，不过我试了下，如果我写一个新的评价，就会刷新为正确的平均分了，所以这里在复制配方的时候应该处理一下。
- 显示菜单名，应该在切换后一直保留，而不是再次切换到此页面又回到默认的不勾选状态。

# 11.19
已完成：
- ✅ 新增功能：在店面管理中，根据库存刷新按钮的左边增加一个开关，显示菜单名，打开的话，就会在配方卡片上显示该配方的默认菜单名称，而非配方名称。
- ✅ 修复酒精度计算不一致问题：统一计算逻辑，只计算容积单位（ml, oz, cl）的原料，其他单位不参与计算。新增数据修复功能，重新计算所有已保存配方的容量和酒精度。

未进行：
- 优化筛选布局，减少空间占用

# 11.17

- 一键滚动按钮其实不是最好用的，我本质上是希望能快速跳转到不同的编辑区域，要不我们这样设计，在右边空白区域，设置几个较明显的圆点符号，竖向排列，用户点击后，就可以跳转到对应的编辑区域。用圆点来作为跳转的触发入口，是为了美观，无需增加任何文字。但为了更为方便，只要用户在非输入状态时，直接按12345等等，就能跳转到当前界面的各个标题位置，相当于点击了之前设计的圆点符号，这样给用户提供了更加快捷的选择。请完善设计，然后实现。

- 配方的编辑页面和预览页面，希望在右下方不那么靠下的地方，弄两个固定的按钮，能够一键回到页面顶部和一键去到底部。
- 做一个评分系统，每款配方的编辑页面最下方，可以多次追加评分和评语，每组数据就是一个满分为5分的评分，和一个评语区，希望界面布局合理美观。评分这块，做成用户既可以直接拖动滑条，也可以在滑条旁边的输入栏直接输入数字的形式，评分只记录小数点后1位。
- 同时在配方的基础信息里，增加一条当前评分的数据，其数值由所有评分的平均值计算而来。注意同步更新配方编辑页面和配方预览页面和PDF导出的信息。


# 11.16

我需要在配方库的筛选功能中，新增一个原料筛选功能，以便于用户可以筛选出使用了某几种原料的配方。这个功能位于使用杯型的下方一行，提供一个区域，来从原料库添加原料，选择原料的功能完全继承配方编辑中原则原料的功能即可，注意用户可以添加多个原料进行筛选。

- 确保店面管理里的价格，是独属于这个店面的数据，并且在PDF导出时也要附上这个正确的信息。

- PDF导出最后的原料汇总清单，希望根据原料类别进行展示，以进行区分。
- PDF导出的详细信息部分，希望能跟基本信息部分那种布局类似，以优化空间占用，将能在一行显示的内容放到一行里去，但要保证排版整齐，最好是跟编辑配方界面的横向、纵向布局保持一致。

配方编辑界面中，我发现刚才开发的时候，我漏掉了原本那个“菜单名称”的数据和功能，请把这块照着原样加回来，位置放在高级信息区域的最底部。

好了，现在，我要同步修订PDF导出的页面布局。
- 之前如果PDF的导出布局中，除了分页功能需要保留，其他功能先都移除。
- PDF导出完全按照配方浏览界面的信息，原封不动地呈现就行。
- 记得要有页码。
- 页眉不需要。
- 以上对PDF导出的修改，要同时应用于单个配方导出，和店面酒单导出。


配方浏览页面进行重新布局：
- 名称：保持原状
- 图片展示区：现在是展示一张图，如果尺寸不合适会以放大的方式展示，这导致很多竖向的图的信息展示不全，我希望这里的区域还是这么大，但展示图是一个竖向的展示区域，贴左侧展示，如果有多张图，根据浏览器的页面宽度自动调节展示几张。
- 剩下的内容分块与布局，跟编辑页面保持一致，但所有数据不可修改，且文本风格跟现在保持一致即可。

配方编辑页面的数据布局进行重新整理，重新分为如下模块：
- 基本信息：包含配方名称中英文，容量、酒精度，以及成品图片（把名字改成叫“附图”）
- 配料列表：保持原状
- 制作步骤：布局保持原状。但需要增加一个像原料列表一样，可以随时挪动不同步骤排序的功能，注意拖动后排序的编号也要随之改变。
- 高级信息：其内容和布局如下
风味标签
引用类型、颜色、使用杯型
建议售价、成本计算（用已有公式自动计算得到，不要新增计算量）、利润率（自动计算得到，如已有计算公式，不要新增计算量）
酒款描述。
- 备注

注意，如果对数据库的老版本数据有影响，需要在设置里的“运行数据修复”里，提供修复功能。
