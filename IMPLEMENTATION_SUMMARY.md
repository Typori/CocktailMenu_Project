# 功能更新日志

## 2025-12-01 更新

### 1. 评分系统增强 ✨

**新增功能：已评分内容支持修改**

- **编辑功能**：在历史评分列表中，每条评分记录现在都有编辑按钮
- **修改界面**：点击编辑后，评分和评语可以直接修改
- **保存/取消**：提供保存和取消按钮，操作更灵活
- **实时更新**：修改后自动重新计算平均评分并更新配方

**修改文件**：
- `src/components/RecipeRatings.tsx`
  - 新增状态：`editingId`, `editRating`, `editComment`
  - 新增函数：`handleStartEdit()`, `handleCancelEdit()`, `handleSaveEdit()`
  - 新增图标：`Edit2`, `Check`, `X`
  - UI改进：编辑模式和显示模式切换

**使用说明**：
1. 在配方详情页面的"评分记录"区域
2. 点击任意历史评分右侧的编辑按钮（铅笔图标）
3. 修改评分值或评语内容
4. 点击"保存"确认修改，或点击"取消"放弃修改
5. 修改后平均评分会自动更新

### 2. 图片管理增强 🖼️

**新增功能：图片顺序调整与封面设置**

- **拖拽排序**：支持拖拽图片调整顺序
- **上下移动**：提供上移/下移按钮，精确控制位置
- **封面标识**：第一张图片自动标记为"封面"
- **视觉反馈**：拖拽时半透明效果，操作更直观
- **操作按钮**：悬停显示拖拽手柄、移动和删除按钮

**修改文件**：
- `src/pages/RecipeEditor.tsx`
  - 新增组件：`SortableImageItem` - 可排序图片项
  - 新增函数：`handleImageDragEnd()`, `moveImage()`
  - 新增图标：`MoveUp`, `MoveDown`
  - 导入策略：`rectSortingStrategy` 用于网格布局
  - UI改进：
    - 封面标识徽章（左上角）
    - 拖拽手柄（右上角）
    - 操作按钮组（底部）
    - 提示文字说明

**使用说明**：
1. 在配方编辑页面的"附图"区域
2. **拖拽排序**：按住图片右上角的拖拽手柄，拖动到目标位置
3. **按钮移动**：悬停在图片上，使用底部的上移/下移按钮
4. **封面设置**：第一张图片将作为配方封面在预览模式中显示
5. 封面图片会在各个列表和详情页面中优先展示

**技术实现**：
- 使用 `@dnd-kit/core` 和 `@dnd-kit/sortable` 实现拖拽
- `rectSortingStrategy` 策略适配网格布局
- 实时更新 `recipe.imageIds` 数组顺序
- 保持图片URL和ID的同步更新

---

## 历史更新

# 简化版PDF导出功能实现总结

## 📋 实现概述

成功实现了简化版PDF导出功能，用户现在可以在配方查看页面和店面酒款管理页面选择导出完整版或简化版PDF。

## ✅ 完成的工作

### 1. 新增工具函数 (`src/utils/pdfExport.ts`)

#### `generateSimpleRecipeHTML()`
- 生成简化版配方HTML
- 只包含：配方名称（中英文）、原料列表、制作步骤
- 使用大字体和清晰的表格布局
- 适合厨房或吧台使用

#### `exportSimpleRecipeToPDF()`
- 导出单个配方的简化版PDF
- 文件命名：`配方名称_简化版.pdf`
- 确保内容在一页内显示

#### `exportSimpleVenueMenuToPDF()`
- 导出店面酒单的简化版PDF
- 每个配方单独一页
- 格式完全一致
- 文件命名：`店面名称_简化版酒单.pdf`

### 2. 修改配方查看页面 (`src/pages/RecipeViewer.tsx`)

#### 界面改动
- 将"导出"按钮改为下拉菜单
- 添加 `ChevronDown` 图标表示可展开
- 使用 `DropdownMenu` 组件

#### 功能改动
- 新增 `handleExportSimplePDF()` 函数
- 导入 `exportSimpleRecipeToPDF` 函数
- 两个导出选项：
  - 完整版PDF（含价格、成本等）
  - 简化版PDF（仅原料和步骤）

### 3. 修改店面酒款管理页面 (`src/pages/VenueMenuTab.tsx`)

#### 界面改动
- 将"导出"按钮改为下拉菜单
- 添加 `ChevronDown` 图标
- 使用 `DropdownMenu` 组件

#### 功能改动
- 新增 `handleExportSimpleVenueMenu()` 函数
- 导入 `exportSimpleVenueMenuToPDF` 函数
- 两个导出选项：
  - 完整版酒单（含价格、成本等）
  - 简化版酒单（仅原料和步骤）

### 4. 更新文档 (`docs/功能说明.md`)

- 更新"PDF导出"章节
- 详细说明两种导出模式的区别
- 添加使用场景说明
- 更新文件命名规则

## 🎯 功能特点

### 完整版PDF
- ✅ 包含所有信息（价格、成本、描述、图片等）
- ✅ 适合客户查看和内部管理
- ✅ 包含配方图片展示
- ✅ 店面酒单包含原料汇总清单

### 简化版PDF
- ✅ 只包含原料和步骤
- ✅ 简洁清爽的排版
- ✅ 大字体，易于阅读
- ✅ 每个配方单独一页
- ✅ 格式完全一致
- ✅ 适合厨房或吧台实际制作使用

## 📁 修改的文件

```
src/utils/pdfExport.ts          - 新增简化版导出函数
src/pages/RecipeViewer.tsx      - 添加下拉菜单和简化版导出
src/pages/VenueMenuTab.tsx      - 添加下拉菜单和简化版导出
docs/功能说明.md                 - 更新文档
```

## 🚀 使用方法

### 单个配方导出
1. 打开配方查看页面
2. 点击右上角"导出"下拉按钮
3. 选择"完整版PDF"或"简化版PDF"
4. PDF自动下载

### 店面酒单导出
1. 进入店面酒款管理页面
2. 点击"导出"下拉按钮
3. 选择"完整版酒单"或"简化版酒单"
4. PDF自动下载

## 💡 使用场景

### 完整版
- 📋 给客户展示完整配方信息
- 💰 内部成本和定价分析
- 📊 培训和管理使用
- 🖼️ 需要展示配方图片

### 简化版
- 👨‍🍳 厨房或吧台实际制作
- 📝 快速查看原料和步骤
- 🎯 专注于制作流程
- 📄 便于翻阅和查找

## ✨ 技术亮点

1. **复用现有代码**：基于现有的 `pdfExport.ts` 扩展，不影响原有功能
2. **独立函数**：简化版和完整版完全独立，互不影响
3. **统一体验**：两个页面使用相同的下拉菜单设计
4. **清晰命名**：文件名清楚标识版本类型
5. **无 Linter 错误**：代码质量良好

## 🎉 测试状态

- ✅ 代码编译通过
- ✅ 无 Linter 错误
- ✅ 开发服务器正常启动
- ✅ 功能逻辑完整

## 📝 后续建议

1. 在实际使用中测试PDF导出效果
2. 根据用户反馈调整排版和字体大小
3. 考虑添加更多自定义选项（如字体大小、页边距等）
4. 可以考虑添加批量导出功能

---

**最后更新**: 2025-12-01
